server:
  port : 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/marceligrabowski/intercity-reservation-system-microservices-config
        native:
          search-locations: file://${CONFIG_GIT_REPO}
